import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    Details: {
      flex: 1,
      backgroundColor: '#fff',
      alignItems: 'center',
      justifyContent: 'center',
    },
  });
